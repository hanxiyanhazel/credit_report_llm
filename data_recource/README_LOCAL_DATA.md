# Local Data Only

Put private input files here on your local machine only.

Examples:

- raw XML reports
- raw PDF reports
- private requirement documents
- temporary local reference materials

This directory is intentionally ignored by Git so that real report data is never pushed to GitHub.
