# Local Generated Output Only

This directory is reserved for locally generated parsing outputs and derived artifacts.

Examples:

- `*.standard.json`
- enriched label outputs
- normalized intermediate files

These files are intentionally ignored by Git because they may contain private report content derived from local inputs.
